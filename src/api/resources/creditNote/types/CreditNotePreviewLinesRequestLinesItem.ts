/**
 * This file was auto-generated by Fern from our API Definition.
 */

export interface CreditNotePreviewLinesRequestLinesItem {
    /** The line item amount to credit. Only valid when `type` is `invoice_line_item`. If invoice is set up with `automatic_tax[enabled]=true`, this amount is tax exclusive */
    amount?: number;
    /** The description of the credit note line item. Only valid when the `type` is `custom_line_item`. */
    description?: string;
    /** The invoice line item to credit. Only valid when the `type` is `invoice_line_item`. */
    invoice_line_item?: string;
    /** The line item quantity to credit. */
    quantity?: number;
    /** A list of up to 10 tax amounts for the credit note line item. Cannot be mixed with `tax_rates`. */
    tax_amounts?: CreditNotePreviewLinesRequestLinesItem.TaxAmounts;
    /** The tax rates which apply to the credit note line item. Only valid when the `type` is `custom_line_item` and cannot be mixed with `tax_amounts`. */
    tax_rates?: CreditNotePreviewLinesRequestLinesItem.TaxRates;
    /** Type of the credit note line item, one of `invoice_line_item` or `custom_line_item` */
    type: CreditNotePreviewLinesRequestLinesItem.Type;
    /** The integer unit amount in cents (or local equivalent) of the credit note line item. This `unit_amount` will be multiplied by the quantity to get the full amount to credit for this line item. Only valid when `type` is `custom_line_item`. */
    unit_amount?: number;
    /** Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12 decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set. */
    unit_amount_decimal?: string;
}

export namespace CreditNotePreviewLinesRequestLinesItem {
    /**
     * A list of up to 10 tax amounts for the credit note line item. Cannot be mixed with `tax_rates`.
     */
    export type TaxAmounts =
        | {
              amount: number;
              tax_rate: string;
              taxable_amount: number;
          }[]
        | "";
    /**
     * The tax rates which apply to the credit note line item. Only valid when the `type` is `custom_line_item` and cannot be mixed with `tax_amounts`.
     */
    export type TaxRates = string[] | "";
    /**
     * Type of the credit note line item, one of `invoice_line_item` or `custom_line_item`
     */
    export type Type = "custom_line_item" | "invoice_line_item";
    export const Type = {
        CustomLineItem: "custom_line_item",
        InvoiceLineItem: "invoice_line_item",
    } as const;
}
