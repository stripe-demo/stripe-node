/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as environments from "../../../../environments";
import * as core from "../../../../core";
import { Order } from "../resources/order/client/Client";
import { Product } from "../resources/product/client/Client";
import { Supplier } from "../resources/supplier/client/Client";

export declare namespace Climate {
    export interface Options {
        environment?: core.Supplier<environments.StripeEnvironment | string>;
        /** Specify a custom URL to connect the client to. */
        baseUrl?: core.Supplier<string>;
        token: core.Supplier<core.BearerToken>;
    }

    export interface RequestOptions {
        /** The maximum time to wait for a response in seconds. */
        timeoutInSeconds?: number;
        /** The number of times to retry the request. Defaults to 2. */
        maxRetries?: number;
        /** A hook to abort the request. */
        abortSignal?: AbortSignal;
        /** Additional headers to include in the request. */
        headers?: Record<string, string>;
    }
}

export class Climate {
    protected _order: Order | undefined;
    protected _product: Product | undefined;
    protected _supplier: Supplier | undefined;

    constructor(protected readonly _options: Climate.Options) {}

    public get order(): Order {
        return (this._order ??= new Order(this._options));
    }

    public get product(): Product {
        return (this._product ??= new Product(this._options));
    }

    public get supplier(): Supplier {
        return (this._supplier ??= new Supplier(this._options));
    }
}
