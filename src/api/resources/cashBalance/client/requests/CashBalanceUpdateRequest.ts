/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * @example
 *     {}
 */
export interface CashBalanceUpdateRequest {
    /** Specifies which fields in the response should be expanded. */
    expand?: string[];
    /** A hash of settings for this cash balance. */
    settings?: CashBalanceUpdateRequest.Settings;
}

export namespace CashBalanceUpdateRequest {
    /**
     * A hash of settings for this cash balance.
     */
    export interface Settings {
        /** Controls how funds transferred by the customer are applied to payment intents and invoices. Valid options are `automatic`, `manual`, or `merchant_default`. For more information about these reconciliation modes, see [Reconciliation](https://stripe.com/docs/payments/customer-balance/reconciliation). */
        reconciliation_mode?: Settings.ReconciliationMode;
    }

    export namespace Settings {
        /**
         * Controls how funds transferred by the customer are applied to payment intents and invoices. Valid options are `automatic`, `manual`, or `merchant_default`. For more information about these reconciliation modes, see [Reconciliation](https://stripe.com/docs/payments/customer-balance/reconciliation).
         */
        export type ReconciliationMode = "automatic" | "manual" | "merchant_default";
        export const ReconciliationMode = {
            Automatic: "automatic",
            Manual: "manual",
            MerchantDefault: "merchant_default",
        } as const;
    }
}
