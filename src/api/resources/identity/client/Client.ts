/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as environments from "../../../../environments";
import * as core from "../../../../core";
import { VerificationReport } from "../resources/verificationReport/client/Client";
import { VerificationSession } from "../resources/verificationSession/client/Client";

export declare namespace Identity {
    export interface Options {
        environment?: core.Supplier<environments.StripeEnvironment | string>;
        /** Specify a custom URL to connect the client to. */
        baseUrl?: core.Supplier<string>;
        token: core.Supplier<core.BearerToken>;
    }

    export interface RequestOptions {
        /** The maximum time to wait for a response in seconds. */
        timeoutInSeconds?: number;
        /** The number of times to retry the request. Defaults to 2. */
        maxRetries?: number;
        /** A hook to abort the request. */
        abortSignal?: AbortSignal;
        /** Additional headers to include in the request. */
        headers?: Record<string, string>;
    }
}

export class Identity {
    protected _verificationReport: VerificationReport | undefined;
    protected _verificationSession: VerificationSession | undefined;

    constructor(protected readonly _options: Identity.Options) {}

    public get verificationReport(): VerificationReport {
        return (this._verificationReport ??= new VerificationReport(this._options));
    }

    public get verificationSession(): VerificationSession {
        return (this._verificationSession ??= new VerificationSession(this._options));
    }
}
