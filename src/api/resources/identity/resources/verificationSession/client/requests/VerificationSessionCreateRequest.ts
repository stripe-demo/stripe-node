/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * @example
 *     {}
 */
export interface VerificationSessionCreateRequest {
    /** A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems. */
    client_reference_id?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: string[];
    /** Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: Record<string, string>;
    /** A set of options for the session’s verification checks. */
    options?: VerificationSessionCreateRequest.Options;
    /** Details provided about the user being verified. These details may be shown to the user. */
    provided_details?: VerificationSessionCreateRequest.ProvidedDetails;
    /** Token referencing a Customer resource. */
    related_customer?: string;
    /** The URL that the user will be redirected to upon completing the verification flow. */
    return_url?: string;
    /** The type of [verification check](https://stripe.com/docs/identity/verification-checks) to be performed. You must provide a `type` if not passing `verification_flow`. */
    type?: VerificationSessionCreateRequest.Type;
    /** The ID of a verification flow from the Dashboard. See https://docs.stripe.com/identity/verification-flows. */
    verification_flow?: string;
}

export namespace VerificationSessionCreateRequest {
    /**
     * A set of options for the session’s verification checks.
     */
    export interface Options {
        /** Options that apply to the [document check](https://stripe.com/docs/identity/verification-checks?type=document). */
        document?: Options.Document;
    }

    export namespace Options {
        /**
         * Options that apply to the [document check](https://stripe.com/docs/identity/verification-checks?type=document).
         */
        export type Document =
            | {
                  allowed_types?: ("driving_license" | "id_card" | "passport")[] | undefined;
                  require_id_number?: boolean | undefined;
                  require_live_capture?: boolean | undefined;
                  require_matching_selfie?: boolean | undefined;
              }
            | "";
    }

    /**
     * Details provided about the user being verified. These details may be shown to the user.
     */
    export interface ProvidedDetails {
        /** Email of user being verified */
        email?: string;
        /** Phone number of user being verified */
        phone?: string;
    }

    /**
     * The type of [verification check](https://stripe.com/docs/identity/verification-checks) to be performed. You must provide a `type` if not passing `verification_flow`.
     */
    export type Type = "document" | "id_number";
    export const Type = {
        Document: "document",
        IdNumber: "id_number",
    } as const;
}
