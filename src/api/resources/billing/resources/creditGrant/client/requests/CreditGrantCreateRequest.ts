/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * @example
 *     {
 *         amount: {
 *             type: "monetary"
 *         },
 *         applicability_config: {
 *             scope: {
 *                 price_type: "metered"
 *             }
 *         },
 *         category: "paid",
 *         customer: "customer"
 *     }
 */
export interface CreditGrantCreateRequest {
    /** Amount of this credit grant. */
    amount: CreditGrantCreateRequest.Amount;
    /** Configuration specifying what this credit grant applies to. */
    applicability_config: CreditGrantCreateRequest.ApplicabilityConfig;
    /** The category of this credit grant. */
    category: CreditGrantCreateRequest.Category;
    /** ID of the customer to receive the billing credits. */
    customer: string;
    /** The time when the billing credits become effective-when they're eligible for use. It defaults to the current timestamp if not specified. */
    effective_at?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: string[];
    /** The time when the billing credits expire. If not specified, the billing credits don't expire. */
    expires_at?: number;
    /** Set of key-value pairs that you can attach to an object. You can use this to store additional information about the object (for example, cost basis) in a structured format. */
    metadata?: Record<string, string>;
    /** A descriptive name shown in the Dashboard. */
    name?: string;
}

export namespace CreditGrantCreateRequest {
    /**
     * Amount of this credit grant.
     */
    export interface Amount {
        /** The monetary amount. */
        monetary?: Amount.Monetary;
        /** Specify the type of this amount. We currently only support `monetary` billing credits. */
        type: "monetary";
    }

    export namespace Amount {
        /**
         * The monetary amount.
         */
        export interface Monetary {
            /** Three-letter [ISO code for the currency](https://stripe.com/docs/currencies) of the `value` parameter. */
            currency: string;
            /** A positive integer representing the amount of the credit grant. */
            value: number;
        }
    }

    /**
     * Configuration specifying what this credit grant applies to.
     */
    export interface ApplicabilityConfig {
        /** Specify the scope of this applicability config. */
        scope: ApplicabilityConfig.Scope;
    }

    export namespace ApplicabilityConfig {
        /**
         * Specify the scope of this applicability config.
         */
        export interface Scope {
            /** The price type that credit grants can apply to. We currently only support the `metered` price type. */
            price_type: "metered";
        }
    }

    /**
     * The category of this credit grant.
     */
    export type Category = "paid" | "promotional";
    export const Category = {
        Paid: "paid",
        Promotional: "promotional",
    } as const;
}
