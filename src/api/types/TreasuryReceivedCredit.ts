/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 * ReceivedCredits represent funds sent to a [FinancialAccount](https://stripe.com/docs/api#financial_accounts) (for example, via ACH or wire). These money movements are not initiated from the FinancialAccount.
 */
export interface TreasuryReceivedCredit {
    /** Amount (in cents) transferred. */
    amount: number;
    /** Time at which the object was created. Measured in seconds since the Unix epoch. */
    created: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description: string;
    /** Reason for the failure. A ReceivedCredit might fail because the receiving FinancialAccount is closed or frozen. */
    failure_code?: TreasuryReceivedCredit.FailureCode;
    /** The FinancialAccount that received the funds. */
    financial_account?: string;
    /** A [hosted transaction receipt](https://stripe.com/docs/treasury/moving-money/regulatory-receipts) URL that is provided when money movement is considered regulated under Stripe's money transmission licenses. */
    hosted_regulatory_receipt_url?: string;
    /** Unique identifier for the object. */
    id: string;
    initiating_payment_method_details: Stripe.TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails;
    linked_flows: Stripe.TreasuryReceivedCreditsResourceLinkedFlows;
    /** Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode. */
    livemode: boolean;
    /** The rails used to send the funds. */
    network: TreasuryReceivedCredit.Network;
    /** String representing the object's type. Objects of the same type share the same value. */
    object: "treasury.received_credit";
    /** Details describing when a ReceivedCredit may be reversed. */
    reversal_details?: Stripe.TreasuryReceivedCreditsResourceReversalDetails;
    /** Status of the ReceivedCredit. ReceivedCredits are created either `succeeded` (approved) or `failed` (declined). If a ReceivedCredit is declined, the failure reason can be found in the `failure_code` field. */
    status: TreasuryReceivedCredit.Status;
    /** The Transaction associated with this object. */
    transaction?: TreasuryReceivedCredit.Transaction;
}

export namespace TreasuryReceivedCredit {
    /**
     * Reason for the failure. A ReceivedCredit might fail because the receiving FinancialAccount is closed or frozen.
     */
    export type FailureCode = "account_closed" | "account_frozen" | "international_transaction" | "other";
    export const FailureCode = {
        AccountClosed: "account_closed",
        AccountFrozen: "account_frozen",
        InternationalTransaction: "international_transaction",
        Other: "other",
    } as const;
    /**
     * The rails used to send the funds.
     */
    export type Network = "ach" | "card" | "stripe" | "us_domestic_wire";
    export const Network = {
        Ach: "ach",
        Card: "card",
        Stripe: "stripe",
        UsDomesticWire: "us_domestic_wire",
    } as const;
    /**
     * Status of the ReceivedCredit. ReceivedCredits are created either `succeeded` (approved) or `failed` (declined). If a ReceivedCredit is declined, the failure reason can be found in the `failure_code` field.
     */
    export type Status = "failed" | "succeeded";
    export const Status = {
        Failed: "failed",
        Succeeded: "succeeded",
    } as const;
    /**
     * The Transaction associated with this object.
     */
    export type Transaction = string | Stripe.TreasuryTransaction;
}
