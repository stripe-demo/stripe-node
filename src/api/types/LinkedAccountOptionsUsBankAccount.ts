/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 *
 */
export interface LinkedAccountOptionsUsBankAccount {
    filters?: Stripe.PaymentFlowsPrivatePaymentMethodsUsBankAccountLinkedAccountOptionsFilters;
    /** The list of permissions to request. The `payment_method` permission must be included. */
    permissions?: LinkedAccountOptionsUsBankAccount.Permissions.Item[];
    /** Data features requested to be retrieved upon account creation. */
    prefetch?: LinkedAccountOptionsUsBankAccount.Prefetch.Item[];
    /** For webview integrations only. Upon completing OAuth login in the native browser, the user will be redirected to this URL to return to your app. */
    return_url?: string;
}

export namespace LinkedAccountOptionsUsBankAccount {
    export type Permissions = Permissions.Item[];

    export namespace Permissions {
        export type Item = "balances" | "ownership" | "payment_method" | "transactions";
        export const Item = {
            Balances: "balances",
            Ownership: "ownership",
            PaymentMethod: "payment_method",
            Transactions: "transactions",
        } as const;
    }

    export type Prefetch = Prefetch.Item[];

    export namespace Prefetch {
        export type Item = "balances" | "ownership" | "transactions";
        export const Item = {
            Balances: "balances",
            Ownership: "ownership",
            Transactions: "transactions",
        } as const;
    }
}
