/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 * A Financial Connections Account represents an account that exists outside of Stripe, to which you have been granted some degree of access.
 */
export interface FinancialConnectionsAccount {
    /** The account holder that this account belongs to. */
    account_holder?: Stripe.BankConnectionsResourceAccountholder;
    /** The most recent information about the account's balance. */
    balance?: Stripe.BankConnectionsResourceBalance;
    /** The state of the most recent attempt to refresh the account balance. */
    balance_refresh?: Stripe.BankConnectionsResourceBalanceRefresh;
    /** The type of the account. Account category is further divided in `subcategory`. */
    category: FinancialConnectionsAccount.Category;
    /** Time at which the object was created. Measured in seconds since the Unix epoch. */
    created: number;
    /** A human-readable name that has been assigned to this account, either by the account holder or by the institution. */
    display_name?: string;
    /** Unique identifier for the object. */
    id: string;
    /** The name of the institution that holds this account. */
    institution_name: string;
    /** The last 4 digits of the account number. If present, this will be 4 numeric characters. */
    last4?: string;
    /** Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode. */
    livemode: boolean;
    /** String representing the object's type. Objects of the same type share the same value. */
    object: "financial_connections.account";
    /** The most recent information about the account's owners. */
    ownership?: FinancialConnectionsAccount.Ownership;
    /** The state of the most recent attempt to refresh the account owners. */
    ownership_refresh?: Stripe.BankConnectionsResourceOwnershipRefresh;
    /** The list of permissions granted by this account. */
    permissions?: FinancialConnectionsAccount.Permissions.Item[];
    /** The status of the link to the account. */
    status: FinancialConnectionsAccount.Status;
    /**
     * If `category` is `cash`, one of:
     *
     *  - `checking`
     *  - `savings`
     *  - `other`
     *
     * If `category` is `credit`, one of:
     *
     *  - `mortgage`
     *  - `line_of_credit`
     *  - `credit_card`
     *  - `other`
     *
     * If `category` is `investment` or `other`, this will be `other`.
     */
    subcategory: FinancialConnectionsAccount.Subcategory;
    /** The list of data refresh subscriptions requested on this account. */
    subscriptions?: "transactions"[];
    /** The [PaymentMethod type](https://stripe.com/docs/api/payment_methods/object#payment_method_object-type)(s) that can be created from this account. */
    supported_payment_method_types: FinancialConnectionsAccount.SupportedPaymentMethodTypes.Item[];
    /** The state of the most recent attempt to refresh the account transactions. */
    transaction_refresh?: Stripe.BankConnectionsResourceTransactionRefresh;
}

export namespace FinancialConnectionsAccount {
    /**
     * The type of the account. Account category is further divided in `subcategory`.
     */
    export type Category = "cash" | "credit" | "investment" | "other";
    export const Category = {
        Cash: "cash",
        Credit: "credit",
        Investment: "investment",
        Other: "other",
    } as const;
    /**
     * The most recent information about the account's owners.
     */
    export type Ownership = string | Stripe.FinancialConnectionsAccountOwnership;
    export type Permissions = Permissions.Item[];

    export namespace Permissions {
        export type Item = "balances" | "ownership" | "payment_method" | "transactions";
        export const Item = {
            Balances: "balances",
            Ownership: "ownership",
            PaymentMethod: "payment_method",
            Transactions: "transactions",
        } as const;
    }

    /**
     * The status of the link to the account.
     */
    export type Status = "active" | "disconnected" | "inactive";
    export const Status = {
        Active: "active",
        Disconnected: "disconnected",
        Inactive: "inactive",
    } as const;
    /**
     * If `category` is `cash`, one of:
     *
     *  - `checking`
     *  - `savings`
     *  - `other`
     *
     * If `category` is `credit`, one of:
     *
     *  - `mortgage`
     *  - `line_of_credit`
     *  - `credit_card`
     *  - `other`
     *
     * If `category` is `investment` or `other`, this will be `other`.
     */
    export type Subcategory = "checking" | "credit_card" | "line_of_credit" | "mortgage" | "other" | "savings";
    export const Subcategory = {
        Checking: "checking",
        CreditCard: "credit_card",
        LineOfCredit: "line_of_credit",
        Mortgage: "mortgage",
        Other: "other",
        Savings: "savings",
    } as const;
    export type SupportedPaymentMethodTypes = SupportedPaymentMethodTypes.Item[];

    export namespace SupportedPaymentMethodTypes {
        export type Item = "link" | "us_bank_account";
        export const Item = {
            Link: "link",
            UsBankAccount: "us_bank_account",
        } as const;
    }
}
