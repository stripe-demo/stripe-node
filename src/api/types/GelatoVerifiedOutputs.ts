/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 *
 */
export interface GelatoVerifiedOutputs {
    /** The user's verified address. */
    address?: Stripe.Address;
    /** The user’s verified date of birth. */
    dob?: Stripe.GelatoDataVerifiedOutputsDate;
    /** The user's verified email address */
    email?: string;
    /** The user's verified first name. */
    first_name?: string;
    /** The user's verified id number. */
    id_number?: string;
    /** The user's verified id number type. */
    id_number_type?: GelatoVerifiedOutputs.IdNumberType;
    /** The user's verified last name. */
    last_name?: string;
    /** The user's verified phone number */
    phone?: string;
}

export namespace GelatoVerifiedOutputs {
    /**
     * The user's verified id number type.
     */
    export type IdNumberType = "br_cpf" | "sg_nric" | "us_ssn";
    export const IdNumberType = {
        BrCpf: "br_cpf",
        SgNric: "sg_nric",
        UsSsn: "us_ssn",
    } as const;
}
