/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 *
 */
export interface BankConnectionsResourceTransactionRefresh {
    /** Unique identifier for the object. */
    id: string;
    /** The time at which the last refresh attempt was initiated. Measured in seconds since the Unix epoch. */
    last_attempted_at: number;
    /** Time at which the next transaction refresh can be initiated. This value will be `null` when `status` is `pending`. Measured in seconds since the Unix epoch. */
    next_refresh_available_at?: number;
    /** The status of the last refresh attempt. */
    status: BankConnectionsResourceTransactionRefresh.Status;
}

export namespace BankConnectionsResourceTransactionRefresh {
    /**
     * The status of the last refresh attempt.
     */
    export type Status = "failed" | "pending" | "succeeded";
    export const Status = {
        Failed: "failed",
        Pending: "pending",
        Succeeded: "succeeded",
    } as const;
}
