/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 * This is an object representing a capability for a Stripe account.
 *
 * Related guide: [Account capabilities](https://stripe.com/docs/connect/account-capabilities)
 */
export interface Capability {
    /** The account for which the capability enables functionality. */
    account: Capability.Account;
    future_requirements?: Stripe.AccountCapabilityFutureRequirements;
    /** The identifier for the capability. */
    id: string;
    /** String representing the object's type. Objects of the same type share the same value. */
    object: "capability";
    /** Whether the capability has been requested. */
    requested: boolean;
    /** Time at which the capability was requested. Measured in seconds since the Unix epoch. */
    requested_at?: number;
    requirements?: Stripe.AccountCapabilityRequirements;
    /** The status of the capability. */
    status: Capability.Status;
}

export namespace Capability {
    /**
     * The account for which the capability enables functionality.
     */
    export type Account = string | Stripe.Account;
    /**
     * The status of the capability.
     */
    export type Status = "active" | "disabled" | "inactive" | "pending" | "unrequested";
    export const Status = {
        Active: "active",
        Disabled: "disabled",
        Inactive: "inactive",
        Pending: "pending",
        Unrequested: "unrequested",
    } as const;
}
