/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 *
 */
export interface BankConnectionsResourceAccountholder {
    /** The ID of the Stripe account this account belongs to. Should only be present if `account_holder.type` is `account`. */
    account?: BankConnectionsResourceAccountholder.Account;
    /** ID of the Stripe customer this account belongs to. Present if and only if `account_holder.type` is `customer`. */
    customer?: BankConnectionsResourceAccountholder.Customer;
    /** Type of account holder that this account belongs to. */
    type: BankConnectionsResourceAccountholder.Type;
}

export namespace BankConnectionsResourceAccountholder {
    /**
     * The ID of the Stripe account this account belongs to. Should only be present if `account_holder.type` is `account`.
     */
    export type Account = string | Stripe.Account;
    /**
     * ID of the Stripe customer this account belongs to. Present if and only if `account_holder.type` is `customer`.
     */
    export type Customer = string | Stripe.Customer;
    /**
     * Type of account holder that this account belongs to.
     */
    export type Type = "account" | "customer";
    export const Type = {
        Account: "account",
        Customer: "customer",
    } as const;
}
