/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 *
 */
export interface TaxProductResourceCustomerDetails {
    /** The customer's postal address (for example, home or business location). */
    address?: Stripe.TaxProductResourcePostalAddress;
    /** The type of customer address provided. */
    address_source?: TaxProductResourceCustomerDetails.AddressSource;
    /** The customer's IP address (IPv4 or IPv6). */
    ip_address?: string;
    /** The customer's tax IDs (for example, EU VAT numbers). */
    tax_ids: Stripe.TaxProductResourceCustomerDetailsResourceTaxId[];
    /** The taxability override used for taxation. */
    taxability_override: TaxProductResourceCustomerDetails.TaxabilityOverride;
}

export namespace TaxProductResourceCustomerDetails {
    /**
     * The type of customer address provided.
     */
    export type AddressSource = "billing" | "shipping";
    export const AddressSource = {
        Billing: "billing",
        Shipping: "shipping",
    } as const;
    /**
     * The taxability override used for taxation.
     */
    export type TaxabilityOverride = "customer_exempt" | "none" | "reverse_charge";
    export const TaxabilityOverride = {
        CustomerExempt: "customer_exempt",
        None: "none",
        ReverseCharge: "reverse_charge",
    } as const;
}
