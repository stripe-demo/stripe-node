/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 *
 */
export interface InvoicesResourceShippingCost {
    /** Total shipping cost before any taxes are applied. */
    amount_subtotal: number;
    /** Total tax amount applied due to shipping costs. If no tax was applied, defaults to 0. */
    amount_tax: number;
    /** Total shipping cost after taxes are applied. */
    amount_total: number;
    /** The ID of the ShippingRate for this invoice. */
    shipping_rate?: InvoicesResourceShippingCost.ShippingRate;
    /** The taxes applied to the shipping rate. */
    taxes?: Stripe.LineItemsTaxAmount[];
}

export namespace InvoicesResourceShippingCost {
    /**
     * The ID of the ShippingRate for this invoice.
     */
    export type ShippingRate = string | Stripe.ShippingRate;
}
