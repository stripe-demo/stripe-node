/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 * An active entitlement describes access to a feature for a customer.
 */
export interface EntitlementsActiveEntitlement {
    /** The [Feature](https://stripe.com/docs/api/entitlements/feature) that the customer is entitled to. */
    feature: EntitlementsActiveEntitlement.Feature;
    /** Unique identifier for the object. */
    id: string;
    /** Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode. */
    livemode: boolean;
    /** A unique key you provide as your own system identifier. This may be up to 80 characters. */
    lookup_key: string;
    /** String representing the object's type. Objects of the same type share the same value. */
    object: "entitlements.active_entitlement";
}

export namespace EntitlementsActiveEntitlement {
    /**
     * The [Feature](https://stripe.com/docs/api/entitlements/feature) that the customer is entitled to.
     */
    export type Feature = string | Stripe.EntitlementsFeature;
}
