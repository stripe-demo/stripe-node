/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 * Some payment methods have no required amount that a customer must send.
 * Customers can be instructed to send any amount, and it can be made up of
 * multiple transactions. As such, sources can have multiple associated
 * transactions.
 */
export interface SourceTransaction {
    ach_credit_transfer?: Stripe.SourceTransactionAchCreditTransferData;
    /** A positive integer in the smallest currency unit (that is, 100 cents for $1.00, or 1 for ¥1, Japanese Yen being a zero-decimal currency) representing the amount your customer has pushed to the receiver. */
    amount: number;
    chf_credit_transfer?: Stripe.SourceTransactionChfCreditTransferData;
    /** Time at which the object was created. Measured in seconds since the Unix epoch. */
    created: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    gbp_credit_transfer?: Stripe.SourceTransactionGbpCreditTransferData;
    /** Unique identifier for the object. */
    id: string;
    /** Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode. */
    livemode: boolean;
    /** String representing the object's type. Objects of the same type share the same value. */
    object: "source_transaction";
    paper_check?: Stripe.SourceTransactionPaperCheckData;
    sepa_credit_transfer?: Stripe.SourceTransactionSepaCreditTransferData;
    /** The ID of the source this transaction is attached to. */
    source: string;
    /** The status of the transaction, one of `succeeded`, `pending`, or `failed`. */
    status: string;
    /** The type of source this transaction is attached to. */
    type: SourceTransaction.Type;
}

export namespace SourceTransaction {
    /**
     * The type of source this transaction is attached to.
     */
    export type Type =
        | "ach_credit_transfer"
        | "ach_debit"
        | "alipay"
        | "bancontact"
        | "card"
        | "card_present"
        | "eps"
        | "giropay"
        | "ideal"
        | "klarna"
        | "multibanco"
        | "p24"
        | "sepa_debit"
        | "sofort"
        | "three_d_secure"
        | "wechat";
    export const Type = {
        AchCreditTransfer: "ach_credit_transfer",
        AchDebit: "ach_debit",
        Alipay: "alipay",
        Bancontact: "bancontact",
        Card: "card",
        CardPresent: "card_present",
        Eps: "eps",
        Giropay: "giropay",
        Ideal: "ideal",
        Klarna: "klarna",
        Multibanco: "multibanco",
        P24: "p24",
        SepaDebit: "sepa_debit",
        Sofort: "sofort",
        ThreeDSecure: "three_d_secure",
        Wechat: "wechat",
    } as const;
}
