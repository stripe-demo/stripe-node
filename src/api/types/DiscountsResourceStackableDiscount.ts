/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 *
 */
export interface DiscountsResourceStackableDiscount {
    /** ID of the coupon to create a new discount for. */
    coupon?: DiscountsResourceStackableDiscount.Coupon;
    /** ID of an existing discount on the object (or one of its ancestors) to reuse. */
    discount?: DiscountsResourceStackableDiscount.Discount;
    /** ID of the promotion code to create a new discount for. */
    promotion_code?: DiscountsResourceStackableDiscount.PromotionCode;
}

export namespace DiscountsResourceStackableDiscount {
    /**
     * ID of the coupon to create a new discount for.
     */
    export type Coupon = string | Stripe.Coupon;
    /**
     * ID of an existing discount on the object (or one of its ancestors) to reuse.
     */
    export type Discount = string | Stripe.Discount;
    /**
     * ID of the promotion code to create a new discount for.
     */
    export type PromotionCode = string | Stripe.PromotionCode;
}
