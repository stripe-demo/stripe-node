/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 *
 */
export interface IssuingAuthorizationVerificationData {
    /** Whether the cardholder provided an address first line and if it matched the cardholder’s `billing.address.line1`. */
    address_line1_check: IssuingAuthorizationVerificationData.AddressLine1Check;
    /** Whether the cardholder provided a postal code and if it matched the cardholder’s `billing.address.postal_code`. */
    address_postal_code_check: IssuingAuthorizationVerificationData.AddressPostalCodeCheck;
    /** The exemption applied to this authorization. */
    authentication_exemption?: Stripe.IssuingAuthorizationAuthenticationExemption;
    /** Whether the cardholder provided a CVC and if it matched Stripe’s record. */
    cvc_check: IssuingAuthorizationVerificationData.CvcCheck;
    /** Whether the cardholder provided an expiry date and if it matched Stripe’s record. */
    expiry_check: IssuingAuthorizationVerificationData.ExpiryCheck;
    /** The postal code submitted as part of the authorization used for postal code verification. */
    postal_code?: string;
    /** 3D Secure details. */
    three_d_secure?: Stripe.IssuingAuthorizationThreeDSecure;
}

export namespace IssuingAuthorizationVerificationData {
    /**
     * Whether the cardholder provided an address first line and if it matched the cardholder’s `billing.address.line1`.
     */
    export type AddressLine1Check = "match" | "mismatch" | "not_provided";
    export const AddressLine1Check = {
        Match: "match",
        Mismatch: "mismatch",
        NotProvided: "not_provided",
    } as const;
    /**
     * Whether the cardholder provided a postal code and if it matched the cardholder’s `billing.address.postal_code`.
     */
    export type AddressPostalCodeCheck = "match" | "mismatch" | "not_provided";
    export const AddressPostalCodeCheck = {
        Match: "match",
        Mismatch: "mismatch",
        NotProvided: "not_provided",
    } as const;
    /**
     * Whether the cardholder provided a CVC and if it matched Stripe’s record.
     */
    export type CvcCheck = "match" | "mismatch" | "not_provided";
    export const CvcCheck = {
        Match: "match",
        Mismatch: "mismatch",
        NotProvided: "not_provided",
    } as const;
    /**
     * Whether the cardholder provided an expiry date and if it matched Stripe’s record.
     */
    export type ExpiryCheck = "match" | "mismatch" | "not_provided";
    export const ExpiryCheck = {
        Match: "match",
        Mismatch: "mismatch",
        NotProvided: "not_provided",
    } as const;
}
