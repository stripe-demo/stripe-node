/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 * An Add Invoice Item describes the prices and quantities that will be added as pending invoice items when entering a phase.
 */
export interface SubscriptionScheduleAddInvoiceItem {
    /** The stackable discounts that will be applied to the item. */
    discounts: Stripe.DiscountsResourceStackableDiscount[];
    /** ID of the price used to generate the invoice item. */
    price: SubscriptionScheduleAddInvoiceItem.Price;
    /** The quantity of the invoice item. */
    quantity?: number;
    /** The tax rates which apply to the item. When set, the `default_tax_rates` do not apply to this item. */
    tax_rates?: Stripe.TaxRate[];
}

export namespace SubscriptionScheduleAddInvoiceItem {
    /**
     * ID of the price used to generate the invoice item.
     */
    export type Price = string | Stripe.Price | Stripe.DeletedPrice;
}
