/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 *
 */
export interface CheckoutAcssDebitMandateOptions {
    /** A URL for custom mandate text */
    custom_mandate_url?: string;
    /** List of Stripe products where this mandate can be selected automatically. Returned when the Session is in `setup` mode. */
    default_for?: CheckoutAcssDebitMandateOptions.DefaultFor.Item[];
    /** Description of the interval. Only required if the 'payment_schedule' parameter is 'interval' or 'combined'. */
    interval_description?: string;
    /** Payment schedule for the mandate. */
    payment_schedule?: CheckoutAcssDebitMandateOptions.PaymentSchedule;
    /** Transaction type of the mandate. */
    transaction_type?: CheckoutAcssDebitMandateOptions.TransactionType;
}

export namespace CheckoutAcssDebitMandateOptions {
    export type DefaultFor = DefaultFor.Item[];

    export namespace DefaultFor {
        export type Item = "invoice" | "subscription";
        export const Item = {
            Invoice: "invoice",
            Subscription: "subscription",
        } as const;
    }

    /**
     * Payment schedule for the mandate.
     */
    export type PaymentSchedule = "combined" | "interval" | "sporadic";
    export const PaymentSchedule = {
        Combined: "combined",
        Interval: "interval",
        Sporadic: "sporadic",
    } as const;
    /**
     * Transaction type of the mandate.
     */
    export type TransactionType = "business" | "personal";
    export const TransactionType = {
        Business: "business",
        Personal: "personal",
    } as const;
}
