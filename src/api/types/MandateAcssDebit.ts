/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 *
 */
export interface MandateAcssDebit {
    /** List of Stripe products where this mandate can be selected automatically. */
    default_for?: MandateAcssDebit.DefaultFor.Item[];
    /** Description of the interval. Only required if the 'payment_schedule' parameter is 'interval' or 'combined'. */
    interval_description?: string;
    /** Payment schedule for the mandate. */
    payment_schedule: MandateAcssDebit.PaymentSchedule;
    /** Transaction type of the mandate. */
    transaction_type: MandateAcssDebit.TransactionType;
}

export namespace MandateAcssDebit {
    export type DefaultFor = DefaultFor.Item[];

    export namespace DefaultFor {
        export type Item = "invoice" | "subscription";
        export const Item = {
            Invoice: "invoice",
            Subscription: "subscription",
        } as const;
    }

    /**
     * Payment schedule for the mandate.
     */
    export type PaymentSchedule = "combined" | "interval" | "sporadic";
    export const PaymentSchedule = {
        Combined: "combined",
        Interval: "interval",
        Sporadic: "sporadic",
    } as const;
    /**
     * Transaction type of the mandate.
     */
    export type TransactionType = "business" | "personal";
    export const TransactionType = {
        Business: "business",
        Personal: "personal",
    } as const;
}
