/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 *
 */
export interface IssuingDisputeCanceledEvidence {
    /** (ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Additional documentation supporting the dispute. */
    additional_documentation?: IssuingDisputeCanceledEvidence.AdditionalDocumentation;
    /** Date when order was canceled. */
    canceled_at?: number;
    /** Whether the cardholder was provided with a cancellation policy. */
    cancellation_policy_provided?: boolean;
    /** Reason for canceling the order. */
    cancellation_reason?: string;
    /** Date when the cardholder expected to receive the product. */
    expected_at?: number;
    /** Explanation of why the cardholder is disputing this transaction. */
    explanation?: string;
    /** Description of the merchandise or service that was purchased. */
    product_description?: string;
    /** Whether the product was a merchandise or service. */
    product_type?: IssuingDisputeCanceledEvidence.ProductType;
    /** Result of cardholder's attempt to return the product. */
    return_status?: IssuingDisputeCanceledEvidence.ReturnStatus;
    /** Date when the product was returned or attempted to be returned. */
    returned_at?: number;
}

export namespace IssuingDisputeCanceledEvidence {
    /**
     * (ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Additional documentation supporting the dispute.
     */
    export type AdditionalDocumentation = string | Stripe.File_;
    /**
     * Whether the product was a merchandise or service.
     */
    export type ProductType = "merchandise" | "service";
    export const ProductType = {
        Merchandise: "merchandise",
        Service: "service",
    } as const;
    /**
     * Result of cardholder's attempt to return the product.
     */
    export type ReturnStatus = "merchant_rejected" | "successful";
    export const ReturnStatus = {
        MerchantRejected: "merchant_rejected",
        Successful: "successful",
    } as const;
}
