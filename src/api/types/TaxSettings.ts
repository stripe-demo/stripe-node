/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 * You can use Tax `Settings` to manage configurations used by Stripe Tax calculations.
 *
 * Related guide: [Using the Settings API](https://stripe.com/docs/tax/settings-api)
 */
export interface TaxSettings {
    defaults: Stripe.TaxProductResourceTaxSettingsDefaults;
    /** The place where your business is located. */
    head_office?: Stripe.TaxProductResourceTaxSettingsHeadOffice;
    /** Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode. */
    livemode: boolean;
    /** String representing the object's type. Objects of the same type share the same value. */
    object: "tax.settings";
    /** The status of the Tax `Settings`. */
    status: TaxSettings.Status;
    status_details: Stripe.TaxProductResourceTaxSettingsStatusDetails;
}

export namespace TaxSettings {
    /**
     * The status of the Tax `Settings`.
     */
    export type Status = "active" | "pending";
    export const Status = {
        Active: "active",
        Pending: "pending",
    } as const;
}
