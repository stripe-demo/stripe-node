/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 * An issuing token object is created when an issued card is added to a digital wallet. As a [card issuer](https://stripe.com/docs/issuing), you can [view and manage these tokens](https://stripe.com/docs/issuing/controls/token-management) through Stripe.
 */
export interface IssuingToken {
    /** Card associated with this token. */
    card: IssuingToken.Card;
    /** Time at which the object was created. Measured in seconds since the Unix epoch. */
    created: number;
    /** The hashed ID derived from the device ID from the card network associated with the token. */
    device_fingerprint?: string;
    /** Unique identifier for the object. */
    id: string;
    /** The last four digits of the token. */
    last4?: string;
    /** Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode. */
    livemode: boolean;
    /** The token service provider / card network associated with the token. */
    network: IssuingToken.Network;
    network_data?: Stripe.IssuingNetworkTokenNetworkData;
    /** Time at which the token was last updated by the card network. Measured in seconds since the Unix epoch. */
    network_updated_at: number;
    /** String representing the object's type. Objects of the same type share the same value. */
    object: "issuing.token";
    /** The usage state of the token. */
    status: IssuingToken.Status;
    /** The digital wallet for this token, if one was used. */
    wallet_provider?: IssuingToken.WalletProvider;
}

export namespace IssuingToken {
    /**
     * Card associated with this token.
     */
    export type Card = string | Stripe.IssuingCard;
    /**
     * The token service provider / card network associated with the token.
     */
    export type Network = "mastercard" | "visa";
    export const Network = {
        Mastercard: "mastercard",
        Visa: "visa",
    } as const;
    /**
     * The usage state of the token.
     */
    export type Status = "active" | "deleted" | "requested" | "suspended";
    export const Status = {
        Active: "active",
        Deleted: "deleted",
        Requested: "requested",
        Suspended: "suspended",
    } as const;
    /**
     * The digital wallet for this token, if one was used.
     */
    export type WalletProvider = "apple_pay" | "google_pay" | "samsung_pay";
    export const WalletProvider = {
        ApplePay: "apple_pay",
        GooglePay: "google_pay",
        SamsungPay: "samsung_pay",
    } as const;
}
