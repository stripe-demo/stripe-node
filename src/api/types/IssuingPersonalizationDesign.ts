/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Stripe from "../index";

/**
 * A Personalization Design is a logical grouping of a Physical Bundle, card logo, and carrier text that represents a product line.
 */
export interface IssuingPersonalizationDesign {
    /** The file for the card logo to use with physical bundles that support card logos. Must have a `purpose` value of `issuing_logo`. */
    card_logo?: IssuingPersonalizationDesign.CardLogo;
    /** Hash containing carrier text, for use with physical bundles that support carrier text. */
    carrier_text?: Stripe.IssuingPersonalizationDesignCarrierText;
    /** Time at which the object was created. Measured in seconds since the Unix epoch. */
    created: number;
    /** Unique identifier for the object. */
    id: string;
    /** Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode. */
    livemode: boolean;
    /** A lookup key used to retrieve personalization designs dynamically from a static string. This may be up to 200 characters. */
    lookup_key?: string;
    /** Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. */
    metadata: Record<string, string>;
    /** Friendly display name. */
    name?: string;
    /** String representing the object's type. Objects of the same type share the same value. */
    object: "issuing.personalization_design";
    /** The physical bundle object belonging to this personalization design. */
    physical_bundle: IssuingPersonalizationDesign.PhysicalBundle;
    preferences: Stripe.IssuingPersonalizationDesignPreferences;
    rejection_reasons: Stripe.IssuingPersonalizationDesignRejectionReasons;
    /** Whether this personalization design can be used to create cards. */
    status: IssuingPersonalizationDesign.Status;
}

export namespace IssuingPersonalizationDesign {
    /**
     * The file for the card logo to use with physical bundles that support card logos. Must have a `purpose` value of `issuing_logo`.
     */
    export type CardLogo = string | Stripe.File_;
    /**
     * The physical bundle object belonging to this personalization design.
     */
    export type PhysicalBundle = string | Stripe.IssuingPhysicalBundle;
    /**
     * Whether this personalization design can be used to create cards.
     */
    export type Status = "active" | "inactive" | "rejected" | "review";
    export const Status = {
        Active: "active",
        Inactive: "inactive",
        Rejected: "rejected",
        Review: "review",
    } as const;
}
